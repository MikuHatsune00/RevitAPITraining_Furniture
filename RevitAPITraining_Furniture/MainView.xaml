<Window x:Class="RevitAPITraining_Furniture.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPITraining_Furniture"
             mc:Ignorable="d" 
             d:DesignHeight="350" d:DesignWidth="300"
        Title="Установка семейства категории мебель">
    <StackPanel>       
        <Label Content="Выбрать уровень из списка"/>
        <ComboBox ItemsSource="{Binding Levels}"
                  SelectedValue="{Binding SelectedLevel}"
                  DisplayMemberPath="Name"
                  Margin="5"/>
        <Label Content="Выбрать типоразмер из списка"/>
        <ComboBox ItemsSource="{Binding FamilyTypes}"
                  SelectedValue="{Binding SelectedFamilyType}"
                  DisplayMemberPath="Name"
                  Margin="5"/>
        <Button Content="Применить" Command="{Binding SaveCommand}"
                Margin="5"/>
    </StackPanel>
</Window>
